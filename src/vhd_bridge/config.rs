//! `vhd_bridge::config` — `BridgeConfig` struct, IPC observability key
//! constants, and the `try_apply_bridge_option` write guard.
//!
//! TODO(task 2.1 / 2.2): real implementation. Task 3.1 only creates this
//! file as an empty stub so that `mod config;` in `mod.rs` resolves.
